# NYB-UAP

Android UAP analysis prototype with a Panopticore-style tactical HUD.

Build:
`buildozer -v android debug`

The live engine intentionally avoids mirroring the camera and separates lightweight motion tracking from heavier model inference.
